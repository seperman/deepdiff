language: python

python:
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
  - "pypy"
  - "pypy3"

sudo: false

install:
  - pip install coveralls
  - pip install nose

script:
  - python setup.py install
  - nosetests --with-coverage --cover-package=deepdiff

after_success:
  - coveralls
